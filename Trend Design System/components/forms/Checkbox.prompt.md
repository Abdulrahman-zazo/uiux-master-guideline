Multi-select control — filter facets, terms acceptance, bulk-row selection.

```jsx
<Checkbox checked={v} onChange={setV} label="Free shipping only" />
<Checkbox checked onChange={fn} label="Save this address" description="Used as your default at checkout" />
<Checkbox indeterminate onChange={fn} label="Select all orders" />
```

6px radius — the one place in Trend with a small radius, so the tick reads clearly.
