The catalogue search control — the only pill-shaped input in Trend, because search is a destination, not a form field.

```jsx
<SearchField value={q} onChange={setQ} onSubmit={run} placeholder="Search abayas, bags, perfume…" />
<SearchField size="lg" shortcut="⌘K" />
```

A value swaps the shortcut hint for a clear ×.
