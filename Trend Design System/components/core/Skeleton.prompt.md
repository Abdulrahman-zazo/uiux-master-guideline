Loading placeholder — a shimmering neutral block. Mirror the real content's shape, never a generic grey box.

```jsx
<Skeleton height={220} radius="var(--radius-card-sm)" />
<Skeleton width="60%" height={14} />
<Skeleton height={40} circle />
```

Needs the `trend-shimmer` keyframes (in `components/core/keyframes.css`).
