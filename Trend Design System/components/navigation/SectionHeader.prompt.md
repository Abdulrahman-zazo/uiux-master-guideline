Marks the start of a content block: eyebrow, display heading, and an optional link + carousel arrow pair on the trailing edge.

```jsx
<SectionHeader eyebrow="New this week" title="Fresh from our ateliers" action="Explore more" onPrev={p} onNext={n} />
```

Titles use the display face (Madani Arabic); everything else on the row uses the UI face.
