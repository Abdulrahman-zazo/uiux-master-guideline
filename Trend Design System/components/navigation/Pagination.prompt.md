Catalogue and table paging — round 32px page buttons with ellipsis collapsing above 7 pages.

```jsx
<Pagination page={p} pages={12} onChange={setP} />
```

The current page is a filled purple circle. On the mobile app, use infinite scroll instead.
