A dark 8px-radius label on hover/focus. For icon-only controls in dense dashboards; `IconButton` already sets `title`, so use this only when you need styled or longer copy.

```jsx
<Tooltip label="Export as CSV"><IconButton icon="download" label="Export" /></Tooltip>
```
