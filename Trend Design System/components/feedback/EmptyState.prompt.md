Nothing-here state — a purple-tint circle glyph, display-face title, one line of copy, one recovery action.

```jsx
<EmptyState icon="shopping-bag" title="Your bag is empty" description="Saved items are waiting in your wishlist."
  action={<Button variant="primary">Start shopping</Button>} />
```

Always offer exactly one way out. Never leave a blank panel.
