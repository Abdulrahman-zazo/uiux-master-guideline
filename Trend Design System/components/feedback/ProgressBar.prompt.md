Determinate progress — free-shipping thresholds, checkout steps, seller profile completion, review histograms.

```jsx
<ProgressBar value={72} label="Profile completion" showValue />
<ProgressBar value={4} max={5} size="sm" />
```

Pill-shaped track and fill, always.
