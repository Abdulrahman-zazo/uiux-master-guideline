Countdown for 429 `Retry-After`, OTP cooldown, OTP expiry and resend windows.

```jsx
<RateLimitTimer seconds={retryAfter} label="حاول مرة أخرى بعد" onDone={unlock} />
```

Clock glyph never mirrors; digits are Latin `mm:ss`.
